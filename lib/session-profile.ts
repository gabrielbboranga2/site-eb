import{canAdmin,getPatenteByRoleId,isAltoComando}from'./patentes';import{encodeSession,sign}from'./auth';import{getUserGroupMemberships}from'./roblox';
export async function createSignedSession(profile:{id:string;username:string;avatar?:string}){const secret=process.env.SESSION_SECRET?.trim();if(!secret)throw new Error('SESSION_SECRET não configurado.');const memberships=await getUserGroupMemberships(profile.id),main=memberships.find(item=>item.groupId===521106467);if(!main)throw new Error('Sua conta não pertence ao Exército.');const patente=getPatenteByRoleId(main.roleId),divisions=memberships.map(item=>({id:item.groupId,name:item.sigla,role:item.roleName,roleId:item.roleId,rankNumber:item.rankNumber})),primary=divisions.find(item=>item.id!==521106467),isCreator=patente?.sigla==='CR';const payload=encodeSession({id:profile.id,username:profile.username,avatar:profile.avatar||'',rank:patente?`[${patente.sigla}] ${patente.nome}`:main.roleName,rankNumber:main.rankNumber,roleId:main.roleId,isCreator,isAdmin:canAdmin(main.roleId)||isCreator,isHighCommand:isAltoComando(main.roleId)||isCreator,division:primary?.name||'EXÉRCITO',divisions,cdpDias:patente?.cdpDias||0,exp:Date.now()+86_400_000});return`${payload}.${await sign(payload,secret)}`}
