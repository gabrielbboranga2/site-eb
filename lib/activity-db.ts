import {db,isDatabaseConfigured} from './db';
import type {Activity} from './manager';
let ready:Promise<void>|null=null;
async function schema(){if(!ready)ready=(async()=>{await db()`CREATE TABLE IF NOT EXISTS manager_activity(id text PRIMARY KEY,tipo text NOT NULL,user_id text NOT NULL,username text NOT NULL,descricao text NOT NULL,autor_id text,autor_username text,timestamp timestamptz NOT NULL DEFAULT now())`})().catch(e=>{ready=null;throw e});await ready}
export async function recordActivity(activity:Omit<Activity,'id'|'timestamp'>,{required=false}:{required?:boolean}={}){if(!isDatabaseConfigured()){if(required)throw new Error('O banco de dados não está conectado. O registro não foi confirmado.');return false}try{await schema();await db()`INSERT INTO manager_activity(id,tipo,user_id,username,descricao,autor_id,autor_username) VALUES(${crypto.randomUUID()},${activity.tipo},${activity.userId},${activity.username},${activity.descricao},${activity.autorId||null},${activity.autorUsername||null})`;return true}catch(e){console.error('Não foi possível registrar a atividade confirmada.',e);if(required)throw e;return false}}
export async function readActivities():Promise<Activity[]>{if(!isDatabaseConfigured())return [];await schema();const rows=await db()`SELECT * FROM manager_activity ORDER BY timestamp DESC LIMIT 2000`;return rows.map(r=>({id:r.id,tipo:r.tipo,userId:r.user_id,username:r.username,descricao:r.descricao,autorId:r.autor_id,autorUsername:r.autor_username,timestamp:new Date(r.timestamp).toISOString()}))}
